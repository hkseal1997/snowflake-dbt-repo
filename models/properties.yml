sources:
  - name: "sahil's sources"
    database: DB_ECOMM
    schema: SC_ECOMM
    tables:
      - name: USERS
      - name: DIM_SALES
      - name: PAYMENT_MODES
      - name: orders
      - name: orders2
      - name: dim_payments
      - name: person_sales
      - name: t

models:
  - name: "sahil3"
    config:
      materialized: 'table'
  - name: "users_dbt"
    config:
      materialized: 'view'
  - name: "users_v2"
    config:
      materialized: 'table'
  - name: "users_v3"
    config:
      materialized: 'view'
  - name: "cust_v4.sql"
    config:
      materialized: "empheremal"