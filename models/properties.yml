sources:
  - name: "sahil's sources"
    database: DB_ECOMM
    schema: SC_ECOMM
    tables:
      - name: USERS
      - name: orders
      - name: t1

models:
  - name: "sahil3"
    config:
      materialized: 'table'
  - name: "users_dbt"
    config:
      materialized: 'view'
  - name: "users_v2"
    config:
      materialized: 'table'
  - name: "users_v3"
    config:
      materialized: 'view'
  - name: "cust_v4.sql"
    config:
      materialized: "empheremal"