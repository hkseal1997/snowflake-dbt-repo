SELECT * FROM {{source("sahil's sources","USERS")}} 
UNION ALL
SELECT * FROM {{source("sahil's sources","USERS")}} 
UNION ALL
SELECT * FROM {{source("sahil's sources","USERS")}} 
UNION ALL
SELECT * FROM {{source("sahil's sources","USERS")}} 
UNION ALL
SELECT * FROM {{source("sahil's sources","USERS")}} 
UNION ALL
SELECT * FROM {{source("sahil's sources","USERS")}} 